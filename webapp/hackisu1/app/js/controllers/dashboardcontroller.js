'use strict';

angular.module('surveyorApp.controllers.dashboard', [])
    .controller('DashboardCtrl', ['$scope', '$location', 'angularFire', 'FBURL', 
        function($scope, $location, angularFire, FBURL) {

        }])